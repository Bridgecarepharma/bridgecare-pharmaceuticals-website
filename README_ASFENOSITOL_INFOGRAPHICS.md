# AsFenositol Product Benefits Infographics Patch

This patch changes only the shared product-page component and adds four AsFenositol infographic image files.
It does not modify `data/products.ts`, Prisma, reviews, checkout, or any other product data.

Copy `components/` and `public/` into the repository root and replace when prompted.
