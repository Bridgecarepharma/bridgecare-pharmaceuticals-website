import {PageHero} from "@/components/Sections";export const metadata={title:"Search"};export default async function Page({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=""}=await searchParams;return <><PageHero eyebrow="Site search" title="Search Bridgecare" text={q?`Results for “${q}”`:"Connect this page to your preferred search service."}/><section className="section"><div className="container prose"><p>Search can be connected to a CMS, Algolia, Typesense or a server-side Next.js endpoint.</p></div></section></>}