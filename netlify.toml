[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--include=dev"

# Payment-link launch: no DATABASE_URL or PAYSTACK_SECRET_KEY required.
