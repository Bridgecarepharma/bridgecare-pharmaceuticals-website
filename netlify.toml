[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--include=dev"

# Required Netlify environment variables:
# DATABASE_URL, PAYSTACK_SECRET_KEY, NEXT_PUBLIC_SITE_URL
